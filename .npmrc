# NPM Publishing Configuration
# Ensure package-lock.json is not published
package-lock=false

# Registry (default to npm registry)
registry=https://registry.npmjs.org/

# Save exact versions
save-exact=true

# Access level (set to 'public' for public packages)
access=public
